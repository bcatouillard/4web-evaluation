# 4web-evaluation

## Context

It's my version of advanced web test.

## Installation

   1. Install dependencies
        ```sh
            npm install
        ```
   2. Start the npm server
        ```sh
            npm start server
        ```